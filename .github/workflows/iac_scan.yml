
---

## 3️⃣ `.github/workflows/iac-scan-checkov.yml` – Pipeline GitHub Actions

Objectif : que les étudiants voient :
- 📥 Checkout du code
- 🔍 Lancement du scanner
- ✅ Job vert ou ❌ rouge selon les vulnérabilités

👉 **Crée les dossiers et fichier :**

- Dossier : `.github/workflows/`
- Fichier : `iac-scan-checkov.yml`

👉 **Contenu à mettre dans `iac-scan-checkov.yml` :**

```yaml
name: IaC Security Scan with Checkov

on:
  push:
  pull_request:

jobs:
  checkov-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Checkov
        run: |
          pip install checkov

      - name: Run Checkov on repository
        run: |
          checkov -d . --soft-fail false
